PORT=3000
TZ=Europe/Amsterdam
DATA_DIR=/app/data
HEADLESS=true
# Protects the dashboard and stored contact details. Required for real booking.
APP_USERNAME=brainwash
APP_PASSWORD=replace-with-a-long-random-password
# Safety gate: real booking also requires an explicit dashboard confirmation.
BOOKING_ENABLED=false
